

<html>

  <body>
    <!-- ... -->

    <div class="container">
    <br>
    <h1>Hey ! </h1>
    <h2> C'est ici que tu peux modifier les malversations !</h2>
    <br>
    </div>

    <div class="container">

    <%= form_tag url_for(action: 'update'), method: "patch" do %>

      <%= label_tag 'title' %>
      <%= text_field_tag 'title', @gossip.title %>
      <br /> <br />

      <%= label_tag 'content' %>
      <%= text_area_tag 'content', @gossip.content %>
      <br /> <br />

      <%= submit_tag "Modifier le gossip" %>

    <% end %>
    </div>

    <br /> <br />
    <% if @gossip.errors.any? %>
      <p>Nous n'avons pas réussi à modifier le potin pour la (ou les) raison(s) suivante(s) :</p>
      <ul>
        <% @gossip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>
    <!-- ... -->
  </body>
</html>