<body>

    <div class="container">
    <br>
    <h1>BIENVENUE ! </h1>
    <h2> Psst .. c'est la page des malversations de la ville : <%= @city_name %> !</h2>
    <br>
    </div>

<div class="container">
    <div class="row row-cols-1 row-cols-md-3 g-4">
    <% @all_gossips.each do |item| %>
        <div class="col">
          <div class="card ">
            <div class="card shadow p-3 bg-white rounded" style="">
              <div class="card-body">
                <h5 class="card-title"><%= item.title %></h5>
                <h6 class="card-subtitle mb-2 text-muted"> de <%= item.user.first_name %></h6>
                <p class="card-text">Description empty</p>
                <% link = "http://localhost:3000/gossips/" + (item.id).to_s %>
                <a href="<%= link %>" class="btn btn-primary">Lien vers le potin</a>
                <% link = "http://localhost:3000/users/" + item.user.first_name %>
                <a href="<%= link %>"class="btn btn-primary">Voir le profil de son auteur</a>
              </div>
            </div>
          </div>
        </div>
    <% end%>
    </div>
  </div>
  </body>
  <br>
</html>